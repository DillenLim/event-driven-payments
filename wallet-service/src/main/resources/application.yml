spring:
  application:
    name: wallet-service
  datasource:
    url: jdbc:postgresql://postgres:5432/payments_db
    username: user
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  kafka:
    bootstrap-servers: kafka:9092
server:
  port: 8081
